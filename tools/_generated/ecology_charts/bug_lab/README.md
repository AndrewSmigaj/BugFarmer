# bug_lab — ARCHIVED (do not tune balance here)

bug_lab is the small FENCED arena used only to OBSERVE individual bug behavior. Its fenced pens are fake,
unnatural conditions, so balance tuned here does NOT transfer to the real zone. Everything in `archive/`
is the old overnight bug_lab parameter sweeps (A*/B*/C*/D*/E*/F*/G* configs) + the P-ECO phase history,
kept for reference only. Real tuning happens in `../village_21_B/`.
