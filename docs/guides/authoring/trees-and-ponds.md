# MOVED — one home per topic

This guide is superseded (2026-06):
- **Lakes, shorelines, docks** → [water.md](water.md) — now primitive-backed
  (`terrain.lake` multi-blob shapes, `shore_dress` per-arc banks, bridge_wood docks).
- **Forests** (masses, edge gradient, clearings, understory, floors) →
  [forest.md](forest.md).

The old text mixed primitive-backed rules with aspirational pseudocode (noise density
fields etc.); the merged guides mark which is which so authors don't reach for
functions that don't exist.
