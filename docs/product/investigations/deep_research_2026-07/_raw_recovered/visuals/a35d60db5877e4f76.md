I'll start by exploring the repository structure and searching for the relevant files in parallel.

---

Key finding already: no `ant_tunnels_30` or `ant_colony_40` save exists. Let me verify contents and check the save/scene structure.

---

No `ant_tunnels_30` save exists (only a doc, a preview, and a brand-new `scene_ant_entrance.py`). No `ant_colony_40` save exists either. Now let me understand the save format and read the key scene builders.

---

Now let me understand the chunk JSON format and read the underground_passages zone builder to find the cliff technique.

---

Excellent, the underground_passages builder is very informative. Now let me extract the actual committed west-edge data and understand the save orientation (Y-flip), plus read the mining-camp cliff technique.

---

I made an indexing error — I printed the y=0 row instead of the x=0 column. Let me fix the extraction and also determine the save's Y-orientation.

---

The x=0 column is almost entirely `dirt_block` walls with only one open cell (y=120). This reveals a Y-flip and possibly a transpose between builder and save coordinates. I need to read the exact save format to interpret correctly.

---

The save has NO Y-flip (save() at zonebuilder.py:408-413 writes builder coords directly). But the committed chunks are dated Jun 25-26 while the builder was rewritten Jul 5 — the save may be stale. Let me render an ASCII map of the west strip of the committed save to see the actual tunnel openings, and pin the orientation.

---

Clear now: in the committed save, the grass surface sits at LOW y (y=0-28) and the ant burrows form a west-central cluster (x≈1-45, y≈120-170) with only ONE clean opening at x=0 (y=120). This is the OPPOSITE Y-orientation from the current builder (grass at y=232-255), and the save (Jun 25-26) predates the builder rewrite (Jul 5). Let me confirm the staleness via git and pin the true world orientation.

---

The committed underground save is confirmed stale AND upside-down vs the current builder: the mine camp (tent y=8-9, anvil y=12) sits at LOW y, so the save has surface/north at y=0; the builder (later commit 620d474) puts surface at high y (`+Y = north`). Now let me pin the true cross-zone Y-mapping and get the bee_meadow north edge.

---

Definitive engine convention found — `nakama/modules/world/zone.go:120`: **"+Y = north, so south edge = y0, north edge = y255."** This inverts the task's stated y-labels. So ant's NORTH edge is y=255 (meets bee's SOUTH edge y=0); ant's EAST edge x=255 meets underground's WEST edge x=0 at equal y. Now let me get bee_meadow's actual south edge, the cross-zone mapping, and the mining-camp cliff technique.

---

Major finding on the NORTH edge: bee_meadow_20's committed SOUTH edge (y=0) still has the OLD dry dirt/stone transition — NOT plain meadow. Let me pin the exact cliff-surface code lines and confirm bee's south gradient in its builder.

---

There's a `_sketch_ant30_cliff.py` in the scenes dir — likely the intended ant cliff geometry. Let me read it plus the exact bee south-gradient code.

---

The `_sketch_ant30_cliff.py` confirms the intended contract (`EAST_ENTER = 232`, "cliff meets the east edge here (mining-zone cliff height)"). Now one final extraction — the exact makeup of bee's south edge (y=0) by x, so the north-edge contract is precise.

---

I now have everything needed. Let me compile the findings.

---

# Edge contracts for `ant_tunnels_30` — findings from committed files

## Two headline caveats before the edges (both load-bearing)

**A. The Y-axis in your task brief is inverted vs. the engine.** The authoritative convention is `nakama/modules/world/zone.go:120`: *"+Y = north, so south edge = y0, north edge = y255."* So:
- ant_tunnels_30's **NORTH edge = y255** (not y0). It meets bee_meadow_20's **SOUTH edge = y0**.
- ant's **SOUTH edge = y0** meets ant_colony_40's north edge (y255).
- ant's **EAST edge = x255** meets underground_passages_31's **WEST edge = x0**, at equal y (x/tangential coordinate is preserved across the seam; only the normal coordinate flips to the opposite edge — `zone.go:118-121`).

**B. `underground_passages_31`'s committed save is STALE and Y-FLIPPED relative to its current builder.** The chunk files are from commit `06f43bd` (Jun 25-26); the builder `zone_underground_passages.py` was rewritten later (commit `620d474`) to the `+Y=north` convention but **never re-saved**. Proof from the committed save: the mine-entrance camp props (`tent` y=8-9, `anvil` y=12, `crate` y=14) and the grass surface sit at **low y (0-28)**, i.e. the save has its surface at the SOUTH edge — upside-down vs. the engine and vs. the builder (which puts surface at high y, `CLIFF_Y=232`). `save()` itself does no flip (`tools/zonegen/zonebuilder.py:408-413`), so this is a genuine data/builder mismatch. **The committed east-neighbor save does NOT contain the tunnel stubs you expect** (details in §2). You must decide whether to match the current builder's *intent* (which requires re-saving underground_passages_31) or the actually-committed bytes.

---

## 4. Does an `ant_tunnels_30` build already exist? — NO save, partial scene

- **No zone save**: there is no `nakama/data/zones/ant_tunnels_30/` directory at all.
- **No full zone builder**: no `zone_ant_tunnels*.py` in `tools/zonegen/scenes/`.
- **What DOES exist (committed, commit `b486c7d`)**:
  - `docs/product/zones/ant_tunnels_30.md` — the rev-2 design doc.
  - `tools/zonegen/scenes/scene_ant_entrance.py` — the entrance *vignette* (building + road + pen + main mouth + side mouths + cliff), a composable `place_ant_entrance(b, ox, oy)` piece, NOT a 256×256 zone. Its own cliff/mouth conventions: cliff = `stone_floor` ground + `stone_block` occupant on a noise-warped edge (`scene_ant_entrance.py:66-73`); tunnel throats = `cave_floor`, carved by clearing `stone_block` (`:75-88`); mouth flanks = `dirt_block` mound forms (`:89-97`).
  - `tools/zonegen/scenes/_sketch_ant30_cliff.py` — a **throwaway** cliff-spine options sketch (A/B/C), renders to scratchpad only. Confirms intent: `EAST_ENTER = 232 # cliff meets the east edge here (mining-zone cliff height)` (`:22`), north=high-y, "curve down and across to SW", and an east stone seam `if x > 250: stone_floor` (`:67-70`).
  - `tools/_generated/previews/zones/ant_tunnels_30/scenes/ant_entrance.png` — preview only.

---

## 1. The four edges

### NORTH — `bee_meadow_20` SOUTH edge (its y0) ↔ ant north edge (y255)

The committed contract is **NOT plain meadow** — the "old south dirtying gradient" is still present (its removal is backlogged, and the bee builder still contains it: `zone_bee_meadow_20.py:119-188`, `gradient_field(b, 0, 0, 255, 36, edge="s", ...)` dirties the southern 36 rows; embedded `rock_mass` at `:132-149`; dead-bush/tumbleweed scatter at `:176-182`).

Actual committed `bee_meadow_20` **y0** row (ground, by x-range):
- `x0-9 water_deep`, `x10-12 water_shallow`, `x13-19 sand` — a **water bay fills the SW corner** of the seam.
- `x20-24 dirt`, then a **dirt field mottled with stone_floor outcrops** all the way across: `stone_floor` patches at x25-28, 77, 85-88, 103-105, 113-115, 125-129, 135-157, 180-183, 190-192, 199-214, and `x252-255 stone_floor`.
- Scattered solid blocks on y0: `dirt_block`/`stone_block` at x=83,87,91,103,128,132,136,137,165,181,203,208,212 (mineable mass edges).

For contrast, bee's **NORTH edge (y255)** is clean meadow (`grass` x12-255, with a `water_deep/shallow/sand` bay at x0-11) — that's the village-facing edge, not yours.

**Contract to build against:** ant's north edge (y255) currently abuts a dry dirt+stone_floor rough strip with a water bay in the SW corner (x0-19). If the meadow's south gradient is later removed (backlogged "ANT-ARC BUILD PREP"), this becomes plain grass — coordinate with that before finalizing.

### EAST — `underground_passages_31` WEST edge (its x0) ↔ ant east edge (x255)

See §2 — this is the important one, and the committed vs. builder split matters.

### SOUTH — `ant_colony_40` — NOT BUILT

- No `nakama/data/zones/ant_colony_40/` save.
- `tools/zonegen/scenes/scene_ant_colony.py` exists but is a **standalone design vignette** (`W,H = 76,92`, `SOIL = {"base":"dirt_block",...}`), not a 256×256 zone build and not saved. No edge contract exists yet. The doc only says "the main tunnel arteries descend; traffic thickens downward" (`ant_tunnels_30.md:40`). **Say so: south neighbor unbuilt; you define this seam.**

### WEST — world edge (no neighbor). Confirmed by grid (col 0) and the doc (`ant_tunnels_30.md:41`).

---

## 2. The east neighbor's tunnel-stub rows — CORRECTED

Your list `y ≈ 96/124/138/158/168/200` comes from the **current builder** `zone_underground_passages.py:154-158`, which carves ant burrows out through the left edge:

```
((30,185),(0,200)), ((24,110),(0,124)), ((40,145),(0,158)),
((30,185),(0,168)), ((24,110),(0,96)),  ((40,145),(50,138))
   carve_tunnel(..., style="natural", width=2, seed=340+i, wobble=...)
```

Corrections and specifics:
- **Only FIVE endpoints reach the west edge x0: y = 96, 124, 158, 168, 200.**
- **`y=138` is NOT a west-edge stub** — that tunnel ends at `(50, 138)`, terminating ~50 cells *inside* the zone; it never touches x0. Drop 138 from the contract list (or treat it as an internal branch only).
- **What the cells are (per builder):** these are **`cave_floor` throats, width ~2**, cut through the **ant dirt seam** — the surrounding wall block is **`dirt_block`, not `stone_block`** (the west/SW of this zone is the soft-dirt "ant territory" seam: `zone_underground_passages.py:75-96`, `is_dirt`/`substrate` → `dirt_block`). Stray ore/stone speckles the seam, but the burrow walls are dirt. Above the cliff line (y≈232-255) the west edge is `grass` surface; below it is dirt_block/stone with the five cave_floor throats.

**BUT — the committed save does not match this.** From the actual committed chunks, the `underground_passages_31` west column x0 is almost entirely solid **`dirt_block` wall** (ground `cave_floor` under a solid `dirt_block` occupant = unwalkable). The **only clean open (occ=None) cell at x0 is world-y≈120**; there is a burrow *cluster* just inside the edge at roughly **x1-45, y≈120-170**, but it does not punch through at 96/124/158/168/200. And the grass surface is at **y0-28** (flipped). So:

| Source | West-edge (x0) openings | Grass surface | Orientation |
|---|---|---|---|
| Current builder `zone_underground_passages.py:155-157` | y = 96, 124, 158, 168, 200 (cave_floor throats through dirt_block) | y≈232-255 (high y = north) | engine-correct (+Y=north) |
| **Committed save (what the server actually loads)** | one clean open cell ~y120; cluster y≈120-170 near x1-45 | y0-28 (low y) | **flipped / stale** |

**Recommendation:** the 96/124/138/158/168/200 list is only realized if `underground_passages_31` is re-saved from its current builder. Before you pin ant's east-edge throats, either (a) re-generate/commit the east neighbor so its west edge actually carries throats at world-y 96,124,158,168,200 (dropping 138), or (b) match the stale committed reality (a single ~y120 opening). Flag this to the owner — it's a real inconsistency, not a reading error.

---

## 3. The "beginning mining zone" cliff — how to continue it into ant_tunnels_30

The "beginning mining zone" = `underground_passages_31` ("*the first underground mining zone*", `zone_underground_passages.py:2`); its surface transition lives in `scene_mine_entrance.py`.

**Technique (NOT `rock_mass`, NOT `gradient_field`):** a **per-column, noise-warped horizontal surface line** `surf_y[x]` separating grass (north, y ≥ surf_y) from solid rock/underground (y < surf_y). Rock below is `stone_block` laid by `fill_solid` over a `stone_block` substrate.

- Definition: `scene_mine_entrance.py:46-51` — `cliff_surface(seed=4)`: `base = H-24`; `surf_y[x] = base + round(5*(noise-0.5)*2)` (±5-row wobble), `noise_field(W, 8, wavelength=10, octaves=2, seed=4)`.
- Zone-scale application: `zone_underground_passages.py:37` `CLIFF_Y = 232`; `:73` `cliff_nf = noise_field(ZW, 8, wavelength=16, octaves=2, seed=4)`; `:107` `surf_y = [CLIFF_Y + round(6*(cliff_nf[0][x]-0.5)*2) for x in range(256)]` → **cliff line at world-y ≈ 226-238, full width**, flattened over the camp span (`:108-109`). Grass painted y≥surf_y (`:112-114`); floor/ore below (`:116-117`, `fill_solid` at `:163`); broken rubble/`standing_stone` at the cliff base `surf_y-1..-2` (`:198-205`).

**So the height/column to continue:** at the shared seam (underground's x0 = ant's x255), the grass↔rock boundary sits at **world-y ≈ 232** (engine-correct; the stale save shows the flipped equivalent y≈28-30). Your zone should bring stone in at the **EAST edge (x255) starting at y≈232 near the top/north**, then curve the boundary **down (decreasing y) and across (decreasing x)** toward the SW — exactly what the throwaway sketch encodes: `_sketch_ant30_cliff.py:22` `EAST_ENTER = 232`, with `x>250 → stone_floor` as a hard east seam (`:67-70`) so the rock butts flush against the mining zone. Above the line (y≈232-255) both zones are grass surface; below it is the rock/soil body. Note the mining zone builds the cliff-face body as **`stone_block`**, while your entrance vignette currently paints the face as `stone_floor`+`stone_block` (`scene_ant_entrance.py:66-73`) — consistent block choice, just confirm you want `stone_block` for the mineable face.

---

### Quick citation index
- Engine Y/north convention: `nakama/modules/world/zone.go:120`
- Save has no Y-flip: `tools/zonegen/zonebuilder.py:408-413`
- East stubs (builder): `tools/zonegen/scenes/zone_underground_passages.py:154-158`; dirt-seam walls `:75-96`; cliff `:37,:73,:107`
- Mining cliff technique: `tools/zonegen/scenes/scene_mine_entrance.py:46-51`
- Bee south gradient (still present): `tools/zonegen/scenes/zone_bee_meadow_20.py:119-188`
- Ant entrance vignette: `tools/zonegen/scenes/scene_ant_entrance.py:66-100`
- Ant cliff sketch: `tools/zonegen/scenes/_sketch_ant30_cliff.py:22,67-78`
- Ant doc: `docs/product/zones/ant_tunnels_30.md` (E-seam `:35-39`, S `:40`, W `:41`, cliff `:112-121`)