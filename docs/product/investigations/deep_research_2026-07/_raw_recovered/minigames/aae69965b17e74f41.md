I'll investigate the BugFarmer repo systematically. Let me start with the key files in parallel.

---

The path is `nakama/modules/world/`. Let me read the shop handler and find key definitions.

---

Now let me read the message definitions, the unmarshalling in match.go, the inventory sync, and the tests.

---

Now let me find the inventory helpers, error mechanism, state hash, and test helpers.

---

The inventory helpers aren't in state.go. Let me find them and the state hash computation.

---

Let me find the state hash computation and remaining type definitions.

---

Let me read the error mechanism, shop/entity type defs, state hash boundary, and find the client-side hash + test helpers.

---

Let me read the test helpers and find the client-side ComputeStateHash to confirm the determinism boundary.

---

Let me read the client-side ComputeStateHash to confirm exactly what the determinism hash covers.

---

Let me read the InventorySlot and PlayerState struct definitions to complete the picture.

---

I have all the evidence needed. Here is my structured verification report.

---

# Verification Report: Atomic `sell_batch` Shop Operation

Repo root confirmed: paths use `nakama/modules/world/` (NOT `nakama/world/` as claimed — the plan's paths are off by a `modules/` segment). All file:line evidence below.

## 1. `handlers_shop.go` structure — file is `nakama/modules/world/handlers_shop.go`

**File length:** 293 lines. All line claims are essentially accurate (line-drift is minimal).

### `handleShopAction` — lines 35-93
- The `switch msg.Op` is at **line 71** (claim ":71" CORRECT).
- Ops supported: `"buy"` (line 72) and `"sell"` (line 81); default → error "Unknown shop action" (lines 83-85).
- **Buy** further sub-switches on `msg.SlotType` (line 73): `"recipe"` → `shopBuyRecipe`, `"book"` → `shopBuyBook`, default → `shopBuy` (lines 74-80). This is more than the plan implies.
- Range check at lines 47-54: reuses the 3.0 allowance (`dx*dx+dy*dy > 9.0`), error "Too far away".
- `resolveShop` at lines 56-60 (nil → "Nothing to trade there").
- Qty clamp at lines 62-68: `qty<=0 → 1`; `qty>999 → 999`.
- **The FullInventorySync echo is at lines 88-91** (claim ":88-91" CORRECT):
```go
if changed {
    if presence, ok := state.Presences[userID]; ok && presence != nil {
        _ = m.sendInventorySync(logger, dispatcher, player, presence)
    }
}
```
Note the method is `sendInventorySync`, and it only fires when `changed == true`.

### `shopSell` — lines 205-246 (claim ":205-246" CORRECT)
Full validation sequence, quoted:

Bug branch (lines 206-225):
```go
if slotType == "bug" {
    if shop.Kind != "bugs" { ... "They don't buy bugs" ... }
    if slot < 0 || slot >= len(player.BugSlots) || player.BugSlots[slot].ItemID != id || player.BugSlots[slot].Count < qty {
        ... "You don't have that" ...
    }
    sp := state.Species[id]
    if sp == nil { ... "Unknown bug" ... }
    if !player.RemoveBugs(slot, qty) { return false }
    player.Coins += int64(sp.SellPrice) * int64(qty)
    return true
}
```

Item branch (lines 228-245):
```go
if slot < 0 || slot >= len(player.ItemSlots) || player.ItemSlots[slot].ItemID != id || player.ItemSlots[slot].Count < qty {
    ... "You don't have that" ...
}
def := state.Entities[id]
if def == nil { ... "Can't sell that" ... }
if !shopBuysItem(shop, id, def) { ... "They don't buy that" ... }
if !player.RemoveItem(slot, qty) { return false }
player.Coins += int64(def.SellPrice) * int64(qty)
return true
```

So the validation order is: (1) slot-range + `ItemSlots[slot].ItemID==id` + `Count>=qty` combined in ONE compound `if` (line 228); (2) item def lookup `state.Entities[id]` (line 232); (3) `shopBuysItem` check at **line 237** (plan claimed :250 — that is where `shopBuysItem` is *defined*, not where it's *called*; the call is line 237). Removal via `player.RemoveItem(slot, qty)` (line 241, direct slot mutation — see §3). Coins credited by `player.Coins += int64(def.SellPrice) * int64(qty)` (line 244). Payout is the ENTITY's `SellPrice` (item) / `species.SellPrice` (bug) — NOT a shop-side price.

### `shopBuy` — lines 98-128 (brief)
Finds `unit` price by scanning `shop.Sells` for matching `e.ID` (lines 100-105); "Not for sale here" if none; "Not enough coins" if `player.Coins < cost`; adds via `player.AddBugs` (bug dealer + live species) or `player.AddItem`; inventory-full aborts WITHOUT charging (lines 115-125); then `player.Coins -= cost` (line 126).

### `ShopActionMessage` struct — `messages.go:382-390`
```go
type ShopActionMessage struct {
    GX       int    `json:"gx"`
    GY       int    `json:"gy"`
    Op       string `json:"op"`              // "buy" | "sell"
    ID       string `json:"id"`              // item or species id
    Qty      int    `json:"qty,omitempty"`   // default 1
    Slot     int    `json:"slot,omitempty"`  // sell: which of the player's slots
    SlotType string `json:"slot_type,omitempty"` // "item" | "bug" (sell)
}
```
Single-line-item shape: one `Slot` + one `ID` + one `Qty`. No batch/array field exists today.

## 2. Where `ShopActionMessage` is unmarshalled — `match.go:1019-1025` (claim ":1020" CORRECT, the `json.Unmarshal` is line 1021)
```go
case OpCodeAction:
    var shMsg ShopActionMessage
    if err := json.Unmarshal(msg.GetData(), &shMsg); err != nil {
        logger.Warn("Invalid shop action from %s: %v", userID, err)
        continue
    }
    m.handleShopAction(logger, dispatcher, worldState, userID, shMsg)
```
**OpCode: `OpCodeAction = 2`** (`messages.go:8` — `OpCodeAction int64 = 2 // Generic interaction`). So the shop op rides the generic interaction opcode 2, discriminated by the JSON `op` field.

## 3. `RemoveItem` / `RemoveBugs` semantics — `inventory.go:104-121` and `34-51` — ORDER-SAFE
```go
func (p *PlayerState) RemoveItem(slotIndex int, count int) bool {
    if slotIndex < 0 || slotIndex >= len(p.ItemSlots) { return false }
    slot := &p.ItemSlots[slotIndex]
    if slot.ItemID == "" || slot.Count < count { return false }
    slot.Count -= count
    if slot.Count <= 0 {
        slot.ItemID = ""
        slot.Count = 0
        slot.Metadata = nil
    }
    return true
}
```
It mutates ONLY the addressed slot in place. **No compaction/shift** of other slots — when a stack empties it just clears that index (leaves an empty hole). `RemoveBugs` (lines 34-51) is identical in shape. **Conclusion: removing from multiple distinct slot indices in any order is safe** — slot indices remain stable across removals. (Contrast: `AddItem`/`AddBugs` DO merge into the first existing same-id stack, so re-adding is not index-stable, but removal is.)

## 4. Coins storage + credit; how client learns balance
- **Storage:** `PlayerState.Coins int64` (`state.go:288` — `Coins int64 // Currency`).
- **Credited** directly in `shopSell` (`handlers_shop.go:223, 244`) and debited in the buy paths.
- **Client learns the balance via `FullInventorySyncMessage`** — there is NO separate coin message. Struct at `messages.go:579-586`:
```go
type FullInventorySyncMessage struct {
    BugSlots          []InventorySlot `json:"bug_slots"`
    ItemSlots         []InventorySlot `json:"item_slots"`
    Coins             int64           `json:"coins"`
    ItemSlotsUnlocked int             `json:"item_slots_unlocked"`
    Intro             bool            `json:"intro,omitempty"`
    KnownRecipes      []string        `json:"known_recipes,omitempty"`
}
```
Yes — it carries `Coins`, full `BugSlots`+`ItemSlots`, an `Intro` flag, and `KnownRecipes`. Sent via `sendInventorySync` (`match.go` ~628-643) on `OpCodeFullInventorySync = 38` (`messages.go:42`). Note the whole message is a full re-broadcast of both slot arrays + coins on every changed shop action (delivered reliably, `true` flag). Also emits a companion `EquipmentUpdateMessage` (match.go:647-650).

## 5. `shopBuysItem` — `handlers_shop.go:250-265` (definition; plan's ":250" = the def line, CORRECT for the def)
```go
func shopBuysItem(shop *ShopData, id string, def *EntityDef) bool {
    if shop.Kind == "bugs" {
        return strings.HasPrefix(id, "dead_")
    }
    for _, b := range shop.Buys {
        if b == id { return true }
        for _, t := range def.Tags {
            if t == b { return true }
        }
    }
    return false
}
```
**"Buys" data shape:** `ShopData.Buys []string` (`entities.go:162`) — a flat list of **item ids OR tags** (matched against both `id` and the item's `def.Tags`). A bug dealer (`Kind=="bugs"`) additionally auto-buys any `dead_`-prefixed carcass and (in `shopSell`'s bug branch) any live species.

**Where the vendor def comes from at runtime:** `resolveShop` (`handlers_shop.go:16-31`) — targets the anchor occupant cell at (gx,gy) via `chunk.GetOccupantCell`, requires `cell.Occupant.Anchor`, then looks up the entity def `state.Entities[cell.Occupant.ID]` and returns `def.World.Shop`. So the shop is **occupant-def-driven** (static `EntityDef.World.Shop` data), NOT mutable station state. `ShopData` type at `entities.go:159-165`; `ShopEntry` at `168-171` (`ID string`, `Price int64`).

## 6. Error/refusal mechanism to the shop client — `sendWorldError` → `ErrorMessage` on `OpCodeErrorMessage = 40`
`handlers_shop.go` calls `m.sendWorldError(dispatcher, state, userID, "...")` throughout (e.g. "Not enough coins" line 112, "They don't buy that" line 238). Definition at `handlers_world.go:702-713`:
```go
func (m *Match) sendWorldError(dispatcher runtime.MatchDispatcher, state *WorldState, userID string, message string) {
    msg := ErrorMessage{Error: message}
    data, _ := json.Marshal(msg)
    if presence, exists := state.Presences[userID]; exists && presence != nil {
        dispatcher.BroadcastMessage(OpCodeErrorMessage, data, []runtime.Presence{presence}, nil, true)
    }
}
```
`ErrorMessage{Error string}` at `messages.go:608-609`; `OpCodeErrorMessage int64 = 40 // S→C: Operation failed` (`messages.go:43`). This is a presence-targeted, reliable string-error channel — a `sell_batch` could reuse it for "skipped lines," though today it carries a single free-text `error` string (no structured per-line payload). Note: it is fire-and-forget (one error text per call); a batch that wanted per-line skip reporting would either send multiple `sendWorldError` calls or need a new structured message.

## 7. Existing Go shop tests — `nakama/modules/world/shop_test.go` (157 lines) + `recipe_unlock_test.go`
`shop_test.go` covers (all call the sub-functions directly on `(&Match{})`, bypassing `handleShopAction`, range check, and unmarshalling):
- `TestShopBuyItem` (buy 2, coin math, inventory add)
- `TestShopBuyInsufficientCoins`, `TestShopBuyInventoryFullNoDeduct` (no coin deduct on fail), `TestShopBuyNotForSale`
- `TestShopSellItem` (line 74 — sells `tomato` x3 from slot 3, asserts coins==15, remaining count==1)
- `TestShopSellNotOwnedRejected` (stale slot mismatch), `TestShopSellItemNotBought` (tag refusal)
- `TestShopSellLiveBug` (bug slot, species sell_price), `TestShopSellBugToItemShopRejected`, `TestShopSellDeadBugToDealer`
- `TestShopArbitrageInvariant` (validateShopArbitrage)

**Test construction pattern a `sell_batch` test should follow** (from `shop_test.go:6-26`):
```go
func shopTestState() (*WorldState, *PlayerState) {
    state := newTestState(50)                    // swarm_population_test.go:32
    state.Players = map[string]*PlayerState{}
    state.Entities = map[string]*EntityDef{
        "tomato": {Category: "resource", SellPrice: 5, Tags: []string{"crop"}}, ...
    }
    state.Species["fly_common"].SellPrice = 7
    p := testPlayer(0, 0, "")                     // combat_test.go:57
    state.Players["p1"] = p
    return state, p
}
func itemShop() *ShopData { return &ShopData{Kind:"items", Sells:[]ShopEntry{{ID:"seed_tomato",Price:10}}, Buys:[]string{"crop","material"}} }
func bugShop()  *ShopData { return &ShopData{Kind:"bugs",  Sells:[]ShopEntry{{ID:"fly_common",Price:20}}} }
```
Supporting helpers: `newTestState(maxSwarm)` (`swarm_population_test.go:32-65`), `testPlayer(x,y,tool)` (`combat_test.go:57-61`), `nopDispatcher{}` (satisfies `runtime.MatchDispatcher`, `combat_test.go:18-28`). A batch test would set several `p.ItemSlots[i]` entries then call the new batch fn on `(&Match{})` and assert coins + each slot. NOTE: there is currently **no test that exercises `handleShopAction` end-to-end** (range check / dispatch / `sendInventorySync`) — tests hit `shopBuy`/`shopSell`/etc. directly.

## 8. Determinism boundary — CONFIRMED: coins/inventory/shop are NOT in the sim hash
Two levels of evidence:

**(a) In-code design assertions (server):**
- `handlers_shop.go:9-12`: "A buy/sell is a per-player coin + inventory transaction — it NEVER enters the deterministic sim hash (like containers)."
- `entities.go:157-158` (ShopData doc): "Purely per-player transaction state — never in the deterministic sim hash."
- `state.go:317-318` (PlayerState char fields): "None of these are sim state — they never enter the bug-sim hash."

**(b) The actual hash computation is CLIENT-side and covers ONLY bug swarm kinematics** — `BugFarmerClient/Assets/Scripts/Entities/SwarmManager.cs:2208-2234` (`ComputeStateHash()`), FNV-1a over, per swarm, per bug: `bug.x, bug.y, bug.vx, bug.vy` only. No coins, inventory slots, or shop state are hashed. The server never computes this hash itself; it collects client-reported hashes for the drift check (`state.go:214-223` `DriftCheck`; `match.go:2995` collects `ComputeStateHash()` at a settled tick; `OpCodeZoneHash`/sample-response path `match.go:2578`). Therefore mutating `Coins`/`ItemSlots`/`BugSlots` in a `sell_batch` cannot desync clients — consistent with the buy/sell path today.

---

## Flagged discrepancies vs. the plan's claims
1. **Path root is wrong:** plan says `nakama/world/...`; actual is **`nakama/modules/world/...`** (all files).
2. **`shopBuysItem` "claimed at :250":** line 250 is the *definition*; the *call site* in `shopSell` is **line 237**. (Def-line claim is fine if that's what was meant.)
3. **Op switch supports more than buy/sell sub-cases:** buy fans out to `recipe`/`book`/default via `msg.SlotType` (lines 73-80) — not just a flat buy.
4. **The echo method is `sendInventorySync`** (not literally "FullInventorySync"); it sends `OpCodeFullInventorySync=38` and ALSO an EquipmentUpdate. Lines 88-91 correct.
5. Everything else (line ~71 switch, :205-246 shopSell, match.go:1020 unmarshal, FullInventorySync carrying coins+intro) is **CONFIRMED**.